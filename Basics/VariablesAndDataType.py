# variables doesn't contain any object but it refers or pointer to an object
a = 5
b = a
# here we creating a variable and then pointing to refernce variable b... that means b also contains same
print(type(a)) #which actually gives the type of the referncing data
# The Rules for identifiers or naming the variables
#      1.It can be either small case or big case letters but they are case sensitive
#       2.It can't be a digit only, no keywords are allowed
#       3.No special characters like @,#,&

